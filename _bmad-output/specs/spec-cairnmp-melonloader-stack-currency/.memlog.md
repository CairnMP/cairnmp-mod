---
topic: CairnMP response to the MelonLoader stack-currency research: verification-first hardening under a fixed net6.0 pin
updated: 2026-08-12T13:16
---

- (direction) Yeray invoked bmad-spec on the completed technical research report; input is rich and pre-sorted, so extraction is direct with no elicitation
- (decision) Slug inherited from the source research folder technical-cairnmp-melonloader-stack-currency-2026-08-12 per the bmad-spec slug rule; spec folder is spec-cairnmp-melonloader-stack-currency
- (decision) Objective reframed from version currency to verification, per the research cross-dimension insight: currency is unreachable while the loader pins net6.0, so verification is the reachable goal
- (constraint) TargetFramework stays net6.0 in all four projects because MelonLoader v0.7.3 hosts IL2CPP mods on the .NET 6 Desktop Runtime; verified in repo, all four csproj already net6.0
- (constraint) Il2CppInterop and HarmonyX versions are supplied by the installed MelonLoader and are not selectable by this project
- (constraint) Microsoft.NET.Test.Sdk must not exceed 17.13.0 because 17.14.0 restores cleanly via AssetTargetFallback then fails at runtime with Could not find testhost
- (constraint) xunit stays on the deprecated v2 line because v3 floors at net8.0
- (constraint) CI must not require game assemblies, secrets or a Cairn install, because generating Il2Cpp refs needs a local game install and a five-minute game launch and is not automatable
- (constraint) Proprietary Cairn, Unity and MelonLoader assemblies are never committed or published, per README, gitignore line 2 and the AGENTS.md policy
- (capability) CAP-1 CI verification of the packet and protocol surface via dotnet test CairnMultiplayerShared.Tests; verified in repo that this project references only CairnMultiplayerShared with no game assemblies in its graph
- (capability) CAP-2 generate-il2cpp-refs.ps1 line 97 stops instructing contributors to commit game-refs; verified verbatim in repo at that line
- (capability) CAP-3 the build warns when the .NET SDK goes end of life via CheckSdkVulnerabilities=true emitting NETSDK1239; guards the build SDK, not the net6 target
- (capability) CAP-4 test tooling sits at its net6.0 ceiling with the ceiling recorded where a future bumper will see it
- (decision) CAP-4 kept in scope but explicitly gated, since the research rates it marginal and says it may plausibly resolve to changing nothing
- (decision) Companions are stack.md for the pinned dependency chain and version ceilings, and accepted-risks.md for the unmitigable net6 EOL exposure and the stripped-assembly licensing question; no diagrams needed
- (decision) AGENTS.md adopted as a companion by relative path; owned by bmad-project-context and not edited here
- (decision) research.md listed under sources because it is fully absorbed; downstream reads SPEC.md plus companions instead
- (note) Repo state verified 2026-08-12: no CI workflows exist (.github holds only FUNDING.yml), Shared.Tests carries Test.Sdk 17.11.1, xunit 2.9.2, runner 2.8.2
- (assumption) CI platform is GitHub Actions; the repo has a .github directory and no workflows, and the research names no platform
- (assumption) The mod must keep loading under the MelonLoader v0.7.x releases players actually have installed, so the net6.0 pin is treated as fixed for the life of this spec
- (question) xunit.runner.visualstudio net6.0 ceiling is not established and blocks CAP-4
- (question) Whether the xunit v2 line has had any security release since 2.9.3 decides whether CAP-4 is worth doing at all
- (question) Whether publishing stripped Cairn assemblies is legally available is a licensing decision that gates whether full-solution CI is ever reachable
- (question) Confirm GitHub Actions as the CI platform for CAP-1
- (event) Self-validate pass 1 coherence PASS: four capabilities each carry intent and success, six constraints each rule something out, five non-goals, success signal is testable via a deliberately corrupted codec test turning CI red, CAP IDs 1-4 unique. Noted that CAP-3 and CAP-4 success criteria name concrete mechanisms, accepted because a testable criterion needs them
- (event) Self-validate pass 2 preservation PASS: all five research dimensions, the four cross-dimension insights and both recommendation tiers landed in SPEC.md, stack.md or accepted-risks.md; every repo-verifiable claim was re-checked against the repo on 2026-08-12 and held
- (event) Preservation deliberate non-carries, recorded rather than silent: research open question 3 (no MelonLoader CI exemplar located) dropped because the research itself states it does not weaken the recommendation, which rests on this repo's own project graph; research open question 6 (CVE sweep against .NET 6 since EOL) dropped from SPEC open questions because no downstream decision turns on it, the mitigation is identical either way since the runtime cannot be patched at mod level
- (event) Wrapper-only content dropped: research process metadata (dimension stop reasons, citation verification table, source appendix, staleness table mechanics). The staleness re-check cadence itself was carried into stack.md as load-bearing
- (direction) Yeray chose the lean route: break SPEC into stories.yaml then run bmad-sprint-planning, skipping bmad-architecture and bmad-create-epics-and-stories because the net6 pin and loader chain are already locked as SPEC constraints and stack.md
- (decision) Stripped-assembly publication confirmed by Yeray as a permanent non-goal rather than something to investigate; CI for CairnMultiplayerMod stays out of scope and accepted-risks.md stands as written
- (direction) Yeray chose manual bmad-build per story for execution; the bmad-loop orchestrator stays installed but unused for this spec, so PowerShell 7 setup is deferred
- (direction) Yeray asked to close CAP-4 now via lookup rather than leaving it gated
- (decision) CI platform confirmed as GitHub Actions from evidence rather than assumption: origin remote is github.com/yerayalfageme-glitch/cairnmp-mod; repo is a fork with a separate upstream remote, which matters for workflow triggers on fork PRs
- (note) Open question closed 2026-08-12: xunit.runner.visualstudio net6.0 ceiling is 3.0.2 (2025-02-07), which explicitly targets net6.0 and net472. 3.1.0 (2025-05-03) bumped to net8.0 and net472, leaving net6.0 as a computed entry only. Latest is 3.1.5
- (note) Runner 3.x runs v2 test projects: the VSTest adapter runs .NET and .NET Core projects from xUnit.net v2 and v3, so runner 3.0.2 with xunit 2.9.3 on net6.0 is a supported combination. Verified before recommending the jump
- (note) Open question closed 2026-08-12: no xunit release after 2.9.3 (2025-01-08). The package is marked legacy and deprecated on NuGet, security-only, with users directed to xunit.v3
- (decision) CAP-4 promoted from gated to actionable, and its scope grew: the runner ceiling is 3.0.2 not 2.8.2, a far bigger jump than the research assumed when it left this unestablished
- (constraint) xunit.runner.visualstudio must not exceed 3.0.2 - 3.1.0 targets net8.0 and leaves net6.0 as a computed AssetTargetFallback entry, the same silent-failure shape as Test.Sdk 17.14.0
- (note) New cross-cutting insight not in the source research: the AssetTargetFallback ceiling pattern occurs in two of the three test packages, not one. Both Test.Sdk and the runner drop net6.0 by bumping explicit targets to net8.0 while net6.0 survives as a computed entry. That makes pinning the durable deliverable and the version bumps the one-off
- (decision) Research verdict that CAP-4 might resolve to changing nothing is superseded: the xunit bump alone is marginal but the runner jump 2.8.2 to 3.0.2 plus Test.Sdk 17.11.1 to 17.13.0 is real movement, and two of three packages need a pin to stay safe
- (event) SPEC.md and stack.md re-derived after the CAP-4 lookup: CAP-4 ungated with the runner ceiling added, a seventh constraint added for runner 3.0.2, both closed questions removed from Open Questions leaving none blocking, and the GitHub Actions assumption restated as evidence from the origin remote
- (event) Self-validate re-run PASS on both passes: four capabilities each still carry intent and success, seven constraints each rule something out, five non-goals, success signal unchanged and still testable. New material preserved into stack.md rather than the kernel, per the companion rule for tabular content
- (direction) Yeray chose no checkpoints on any story: spec_checkpoint and done_checkpoint are false throughout, so both fields are omitted and take their schema defaults
- (decision) Execution order puts CAP-1 CI first and CAP-4 package bumps last, so the CI workflow exists as the safety net that verifies the version bumps rather than the bumps landing unverified
- (decision) Story 1 is the GitHub Actions workflow running dotnet test CairnMultiplayerShared.Tests -c Release, satisfying CAP-1
- (decision) Story 2 is the generate-il2cpp-refs.ps1 line 97 completion-message fix, satisfying CAP-2
- (decision) Story 3 is setting CheckSdkVulnerabilities=true, satisfying CAP-3
- (decision) Story 4 is raising and pinning the three test packages to their net6.0 ceilings, satisfying CAP-4
- (event) stories.yaml written and checked against all four schema validity rules: parses as a top-level list, four stories with unique quoted string ids 1 to 4, all required fields present, ids prefix-free, no status field anywhere. PASS
